### ✅ **Backend: `backend/README.md`**

```markdown
# NID Project Showcase – Backend

This is the backend API for the NID Project Showcase, built with **Node.js**, **Express**, and **MongoDB**, and deployed on **Render**.

## 🛠️ Tech Stack

- Node.js
- Express.js
- MongoDB (Mongoose)
- CORS
- dotenv
- Render (for hosting)

## 🗂️ API Endpoints

### `GET /api/projects`
Fetch all projects.

### `POST /api/projects`
Add a new project.

### `GET /api/projects/:id`
Fetch a single project by ID
